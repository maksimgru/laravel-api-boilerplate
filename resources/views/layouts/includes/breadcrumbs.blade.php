<ol class="hidden breadcrumb mt-4 mb-4">
    <li class="breadcrumb-item active">
        <a href="{{ route($route_constants::ROUTE_NAME_WEB_HOME) }}" title="{{ __('Home') }}">{{ __('Home') }}</a> / page1 / page2
    </li>
</ol>
